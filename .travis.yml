language: csharp
dotnet: 2.0.3
mono: none
sudo: false
cache:
  directories:
  - "$HOME/.nuget/"
before_install:
- set -e
addons:
  apt:
    packages:
    - dotnet-hostfxr-2.0.3
script:
- "bash ./$CODEBASE/scripts/build"
